# nano — 引き継ぎ

ローカル永続AIコンパニオン。**このファイルを最初に読むこと。**
細かい仕様は `docs/` にある。ここには、コードを読んだだけでは分からないこと
（なぜそう作ったか、何を壊してはいけないか、何を実測したか）だけを書いてある。

---

## 1. 第一原理

> **人格と記憶は「モデルの重み」ではなく「自分のディスク上のデータ」に宿らせる。**
> モデルはいつでも交換可能な臓器で、記憶ストアのほうが本体（＝魂）。

設計判断で迷ったら、ここに戻る。**この原理に反する変更は、便利でも入れない。**
クラウドAIはサービスが終われば人格ごと消える。その逆をやるための構造なので、
利便性のために依存を増やすと、プロジェクトの存在理由が消える。

---

## 2. 現在地

| | 状態 |
|---|---|
| M0 骨格 | ✅ 設定 / SQLite / llama-server クライアント / 埋め込み / 推論ゲート |
| M1 記憶コア＋CUI | ✅ 書き込みパイプライン・想起・忘却・人格プロンプト・記憶ベンチ |
| M2 無意識デーモン | ✅ 常駐して記憶化・連想・気づき・忘却・整理。外界取り込みと人格変更の承認フロー |
| M2.5 実機対応 | ✅ 埋め込み同一性の記録とキャリブレーション |
| M3 グラフ可視化 | ✅ 記憶グラフのビューア（依存ゼロ・外部読み込みゼロ） |
| **M4 人格の固定** | 🟨 計測（`nano probe`）と教師データ収集（`/star` `/again` → `nano dataset`）は動く。QLoRA は**設計まで**（`docs/finetune.md`）。当てるのは実機待ち |
| **M5 偏在化** | ⬜ 音声・アバター・自発発話（`impulse`）・スマホから |

- コード約 6,500 行 / テスト約 3,000 行 / **214 件すべてオフラインで通る**
- ブランチ: `claude/denshi-maid-nano-design-x441c3`
  （前: `claude/load-and-execute-cfjy3q` → `claude/local-persistent-ai-companion-hvbwap`）

### まだ一度もやっていないこと（重要）

**ユーザーの実機（llama-server + multilingual-e5 + GPU）で動かしていない。**
サンドボックス内では訓練済みの日本語埋め込みモデルまでは通した（§7）。
HTTP 経路そのものは偽サーバー（`tests/fake_server.py`）で通してある
（URL の組み立て・SSE・JSON の拾い方・エラー・中断・プロキシ環境）。

**残っているのは本物のモデルでしか分からないこと:** プロンプトの実挙動、
JSON の壊れ方の実際、生成速度、VRAM 実測値、想起の質。

---

## 3. 壊してはいけないもの（禁則）

コードを変える前にここを読む。どれも一度は破りかけて、破ると静かに壊れる。

1. **生ログ（`events`）を削除する経路を作らない。** 忘却は `notes.state` の遷移
   （active → cold → merged）だけで表す。行は消えない。
   「消えて困る記憶が消える」のは一生モノとして致命的。
2. **平文ミラーを維持する。** `soul/archive/*.jsonl` と `soul/export/*.md`。
   このコードが全部消えても記憶が読める状態が「一生自分のもの」の実体。
3. **依存を増やさない。** いま `httpx` と標準ライブラリだけ（numpy は任意）。
   LangChain / LlamaIndex / Letta / mem0 / FastAPI / Cytoscape は**意図的に不採用**。
   抽象化層の寿命がこのプロジェクトの寿命（一生）より短い。
4. **外部ネットワークに依存する表示物を作らない。** グラフビューアが CDN を使わないのは
   このため。ネットが無いと魂が見えなくなる作りにしない。
5. **無意識に人格を書き換えさせない。** `current_focus` / `mood` は自動でよいが、
   `identity` / `user_model` は `proposals` に積むだけ。反映は `nano review` で人間が承認し、
   監査ログに `updated_by='human'` として残る。
6. **異なる埋め込み空間のベクトルを混ぜない。** `meta.embedding_identity` で照合し、
   食い違えば起動を止める。差し替えは `nano reembed` が正規手順。
7. **コサインの絶対値をしきい値に使わない。** モデルごとに帯が違う（§7）。
   σ 単位で持ち、`nano calibrate` で実測する。
8. **オフラインで全テストが通る状態を保つ。** `OfflineLLM` + `HashEmbedder` で
   全系が回る。ローカルLLMが無くても回帰を検出できることが、長く手を入れる前提。
9. **教師データのプロンプトを後から組み直さない。** ⭐ は「応答」ではなく
   「そのとき渡した messages と応答の対」で保存する。組み直すと想起される記憶が変わり、
   モデルが見ていない材料から答えを出す訓練＝**幻覚の訓練**になる。憲章の最重要禁則と衝突する。
   だから ⭐ はいま話している会話の中にしか打てない（`App.star` が構造的に塞いでいる）。
10. **記憶を重みに焼かない。** QLoRA の loss は **assistant の応答部分にだけ**かける
   （completion-only）。⭐ の system プロンプトには想起された記憶が本文のまま入っているので、
   全体に loss をかけると記憶が重みに入る。第一原理に正面から反し、かつ
   「記憶に無いことを答える」訓練になる。効率の話ではなく原理の話。`docs/finetune.md`
11. **測定条件の違う数字を並べない。** 人格の基準（`soul/persona_baseline.json`）には
   埋め込み空間・対話モデル・アダプタ・憲章のハッシュを刻む。**埋め込み空間が違えば
   比較を断る**（禁則6の人格版）。対話モデルやアダプタの違いは止めない——
   それを測るのが `nano probe` の用途なので、差分を添えて出す。

---

## 4. コードの地図

```
src/nano/
├── app.py            全部を束ねる。CUI もデーモンもUIもここが入口
├── config.py         config.toml の読み込み（未知キーは無視 = 前方互換）
├── llm.py            OpenAI互換クライアント。CancellableLLM で背景ジョブに中断を配る
├── embed.py          ServerEmbedder（実機）/ HashEmbedder（オフライン）。identity を名乗る
├── vectors.py        float32 BLOB。numpy があれば使い、無ければ純Python
├── gate.py           InferenceGate（プロセス内）/ SharedInferenceGate（プロセス間）
├── calibration.py    そのモデルの類似度分布を実測し、しきい値をσ単位にする
├── offline.py        GPU無しで全系を通すスタブLLM。**テストの土台**
├── doctor.py         実機に載せる前の点検。何も直さない（直し方だけ出す）
├── logs.py           デーモンのログ。日次ローテーション（常駐側が持たないと成立しない）
├── store/            記憶ストア。db / events / episodes / notes / graph / entities
│                     / state / jobs / proposals / identity / stars / archive / schema.sql
├── memory/           pipeline（書き込み）/ retrieve（想起）/ decay（忘却）
│                     / reembed / prompts
├── unconscious/      daemon.py + jobs/（write, associate, reflect, decay, curate, ingest）
├── persona/          compose（システムプロンプト構築）/ probes / drift（測定条件つきの
│                     ドリフト計測）/ dataset（⭐→教師データ。世代を混ぜない）
└── viewer/           data（グラフ組み立て）/ page / server / index.html

train/                nano の外。QLoRA の骨（masking / prepare / qlora）。別 venv で回し、
                      nano からは import しない。masking.py だけ tests/ から検査される
```

魂は `soul/` に閉じている。**このディレクトリを丸ごとコピーすれば引っ越しとバックアップ。**
`.gitignore` 済み（魂を共有リポジトリに置かない）。

---

## 5. 動かす

```bash
pip install -e ".[fast,dev]"

# オフラインで全部動く（LLMもGPUも不要。まずこれで挙動を確かめる）
python -m nano --offline chat
python -m nano --offline daemon --once --now   # --now は間隔とアイドルを無視
python -m nano --offline graph --export /tmp/g.html
pytest                                          # 全件オフライン
python tests/bench/memory_bench.py              # 記憶ベンチ

# 実機
llama-server -m models/Qwen3-14B-Q4_K_M.gguf -c 8192 --port 8080
llama-server -m models/multilingual-e5-large-Q8_0.gguf --embedding --port 8081
python -m nano calibrate    # 初回必須。飛ばすと絶対しきい値のまま壊れる
python -m nano chat
```

主なコマンド: `chat` `daemon` `doctor` `jobs` `review` `sleep` `decay` `recall` `graph`
`calibrate` `reembed` `probe` `leak` `stars` `dataset` `state` `stats` `export` `backup`
`chat` の中では `/why` `/recall` `/focus` `/again` `/star` `/avoid` `/unstar` `/stars`
`/sleep` `/decay` `/stats`

---

## 6. 判断の記録（なぜそうなっているか）

コードを見ても分からない、途中で**変えた**判断。同じ轍を踏まないために残す。

| 当初の案 | 実際 | なぜ変えたか |
|---|---|---|
| 記憶レイヤーに mem0 / Letta / cognee | **自作** | Zep が完全自己ホストから後退した実例がある。魂を他人のロードマップに預けない |
| 忘却＝削除 | **cold化＋統合（不可逆削除なし）** | 人間らしく忘れつつ取り返しがつく必要がある |
| 無意識を別プロセスの別モデルで | **1モデルを優先度付きゲートで譲り合う** | VRAM 8〜16GB では2モデル常駐できない |
| `InferenceGate`（threading） | **`SharedInferenceGate`（SQLiteリース）** | デーモンが別プロセスになった瞬間、threading のロックは届かない |
| ファイルロックで排他 | **SQLite の1行** | `fcntl` が Windows で使えない。常駐先が Windows |
| 無意識が identity を更新 | **提案 → 人間が承認** | 気づかないうちに別人になっているのを防ぐ構造上の歯止め |
| FastAPI + Cytoscape.js | **http.server + 自前Canvas** | CDN依存はネットが無いと魂が見えなくなる。依存も増える |
| グラフの色＝カテゴリ | **色＝誰が作った記憶か（3色）** | 任意の2点が隣り合うグラフで色覚検証を通るのは3色まで。そして実際に見たいのは分類ではなく「自分が話した/無意識が考えた/外から来た」の区別 |
| 密度はしきい値で制御 | **`link_max_per_note`（上限）で制御** | しきい値では崖になる（§7）。上限はモデルに依らず効く |
| ログの日分けは起動バッチのリダイレクトで | **デーモン自身が日次ローテーション** | バッチの `%date%` は起動時に一度決まるだけ。3週間動けば3週間ぶんが「起動日」の1ファイルに入って際限なく太る |
| ⭐ は応答に付ける印 | **⭐ はプロンプトと応答の対** | 後で組み直すと想起結果が変わっている。その差で学習すると幻覚を教えることになる。代償として、過去ログを遡って ⭐ は付けられない |
| `nano dataset` は警告を出すだけ | **ベースラインが無ければ拒否** | 「計測が先」を注意書きにすると必ず飛ばされる。`nano review` と同じく構造上の歯止めにした |
| ORPO の rejected はベースモデルに生成させる | **`/again` で人間が対を作る** | 出し直しは前回と同じ messages で行うので、応答の差だけが残って対になる。組み直すとプロンプトの差を学ぶことになる |
| 出し直された応答も記憶にする | **`meta.replaces` を見て記憶から外す** | 撤回した言葉を事実として覚えると想起が濁る。ただし生ログからは消さない（禁則1）。エピソードには紐づけて未処理から外すだけ |
| 学習コードを nano に入れる | **`train/` に別 venv で外置き** | torch / peft / bitsandbytes を依存にすると禁則3に反する。それ以上に、学習スタックが nano の起動条件になると10年後に起動しなくなる。LoRA は失われても人格が死なないもの＝魂の一部ではない |
| LoRA を積み上げて v2, v3 と育てる | **毎回ベースモデルから作り直す** | 積むとどの層が何を持っているか分からなくなり、「全部外しても同じ存在が立ち上がる」を確かめられなくなる。それが確かめられない時点で、人格の本体は憲章と記憶ではなくなっている |
| 人格の基準はファイルが在れば良い | **測定条件つきで記録し、空間が違えば比較を断る** | `--offline` で取った基準を実機でそのまま使うと、ハッシュ埋め込みと e5 のベクトルを cos にかけた数字が「人格のずれ」として出る。どちらも既定1024次元なので次元チェックに引っかからない |

### 実際に踏んだ罠（再発させない）

- **リンク次数を正規化せずスコアに足した** → ハブ記憶が何を聞いても上位に来た
- **重要度→半減期が線形だった** → 30日で「妹の名前」まで忘れた（ベンチが検出）
- **連想強化を一律にかけた** → どうでもいい記憶が巻き添えで生き延びた
- **`ingest` を高優先度にした** → 毎tick積まれて他のジョブを永久に押しのけた
- **リポジトリ直下の `__init__.py`** → pytest がリポジトリ全体をパッケージ扱いし、
  `import bpy` で**テストが1件も走らなくなっていた**（`attic/` へ移動して解決）
- **ラベル衝突・キャンバス歪み** → スクリーンショットを撮るまで気づかなかった
- **`LlamaServerLLM` にテストが1件も無かった** → 全テストが `OfflineLLM` を通っており、
  HTTP クライアントは**一度も実行されていなかった**。偽サーバーを立てて塞いだ
- **`HTTP_PROXY` があると localhost にもプロキシが挟まる** → 「llama-server に
  繋がらない」という嘘のエラーが出る。両クライアントを `trust_env=False` にした
- **人格の基準に測定条件が無かった** → `--offline` で取った基準を実機に持ち込むと、
  別空間のベクトルどうしを比べた数字が平然と表示される。しかも `nano dataset` の
  歯止めは「ファイルが在るか」しか見ていなかったので、**スタブで取った無意味な基準でも
  通り抜けていた**。記憶ストア側（禁則6）は塞いであったのに、人格側だけ空いていた
- **`CREATE TABLE IF NOT EXISTS` では既存の soul.db に列が増えない** → 一生モノの DB に
  あとから列を足せない。`Database.ADDED_COLUMNS` を通す（消す方向のマイグレーションは書かない）

> **教訓: 半分は「走らせるまで見えなかった」。**
> 記憶の品質はベンチで、UI は実描画で、埋め込みは実モデルで確かめること。
> コードを読んで正しそうでも、それは何の保証にもならない。

---

## 7. 実測値

### 埋め込みの類似度分布（無関係な文どうし）

| モデル | 平均 | σ | 出どころ |
|---|---|---|---|
| ハッシュ埋め込み（`--offline`） | **+0.012** | 0.043 | 実測 |
| 訓練済み日本語モデル（spaCy 300次元） | **+0.776** | 0.063 | 実測 |

同じ `link_min_similarity = 0.32` が、前者では「ほとんど通さない」、後者では **100% を通す**。
**絶対しきい値は移植できない。** multilingual-e5 も後者と同じ帯に来るはず。

### しきい値では密度を制御できない（σスイープ・記憶20件）

| σ | 実効しきい値 | 1記憶あたりのリンク |
|---|---|---|
| 2.0 | 0.902 | 2.5 本 |
| 2.5 | 0.934 | **0.3 本** |

崖になる。乱数ペアで測った分布から決めたしきい値を、**上位k件という偏ったペア**に
当てているため、しきい値は必ず分布の尾に置くことになる。
→ `link_max_per_note`（既定3）で制御する。上限 2/3/4 で 2.9/4.0/4.8本 と単調。
`nano stats` の `links_per_note` が **10本を超えていたら緩すぎる**。

### 記憶ベンチ（オフラインスタブ・M1以降ずっと維持）

- 想起率: 0日後 91.7% / 90日後 **83.3%**
- 忘却率: 雑事の **75%** が非アクティブ、重要な記憶は 10/12 がアクティブのまま

### 検証の限界

spaCy の平均語ベクトルは**帯の位置は e5 系と同じだが、関連ペアを引き離す力がずっと弱い**
（関連あり 0.703 / 関連なし 0.78 と逆転する例もあった）。
検証できたのは「しきい値が移植できないこと」と「密度の制御構造」まで。
**想起の質そのものは実機の e5 で測り直すこと。**

---

## 8. 次の一手

### 最優先: 実機で一度動かす

ユーザーの手元でしかできない。見せてもらうべき出力:

```bash
python -m nano doctor      # まずこれ。繋がるか・次元・ものさし・基準をまとめて診る
python -m nano calibrate   # 平均・σ・実効しきい値
python -m nano stats       # links_per_note（10を超えていないか）
python -m nano graph       # ハリボールになっていないか
```

これが揃うまで、σ の既定値（1.5 / 1.0 / 2.0）と
`reflect_importance_threshold = 3.0` は**勘のまま**。実データが来たら詰められる。

**M4 と同時に進めてよい。むしろ M4-3 の判定基準は実機の数字が無いと決まらないので、
片方だけでは止まる。** ただし順番だけは守ること:

```
nano doctor          # 人格ベースラインが FAIL なら、それはスタブで取ったもの
nano calibrate       # ものさし
nano probe --save-baseline   # ← 実機のベースモデルで、アダプタを外した状態で取る。ここが原点
（会話しながら /star を貯める）
nano dataset → 学習 → nano probe
```

`nano probe --save-baseline` を**素のモデルで**取ることが全ての前提。
LoRA を当てたあとに取り直すと、比べる相手が消えて M4-4（再計測）が成立しなくなる。

### M4（人格の固定）

順番を守ること。1 と 2 は入った。

1. ✅ ドリフト計測（`nano probe`、固定20問）
2. ✅ ⭐（`/star` `/avoid` `/unstar` `/stars` `/again` → `nano stars` → `nano dataset`）
   - 保存されるのは**プロンプトと応答の対**。理由は §3 の禁則9
   - `/again` は**前回と同じ messages で**出し直す。想起をやり直さないので、
     2つの応答が「同じ入力に対する別々の出力」になり ORPO の対として成立する
   - `nano dataset` はベースラインが無いと止まる（`--force` で飛ばせるが既定では止まる）
   - 出力は `soul/export/train/{sft,avoid,orpo}.jsonl` ＋ `manifest.json`。素の JSONL で、
     特定の学習フレームワークに寄せていない
3. 🟨 ⭐ 付きだけを教師データに QLoRA（8〜16GB なら 8B が現実的）。
   **設計は `docs/finetune.md` に書いた。当てるのは実機待ち。** 要点だけ:
   - 焼くのは訛りだけ。**記憶と憲章は焼かない**（禁則10 = completion-only loss）
   - 学習コードは nano に入れない。`train/` に別 venv（骨は置いた。実機で未実行）
   - 対話用の OpenAI 互換 API にはアダプタが出てこないので `[llm] adapter` に人間が名札を書く。
     それが計測記録と ⭐ の1件ごとに刻まれる。`nano doctor` が llama-server の
     `/lora-adapters` と突き合わせて裏を取る（裏取りできない版のサーバーでは諦めるだけ）
   - 判定は平均類似度ではなく**カテゴリ別**。style は動いてよく、
     identity / value / memory は動いてはいけない
   - **記憶の焼き付き検査**（想起を切って固有名詞を問う）を必ず通す
   - LoRA を積み上げない。世代を混ぜない
4. ⬜ **ORPO 寄り・低rank・小データ。** SFT を大量に回すと persona drift が出る
   （[arXiv:2601.12639](https://arxiv.org/pdf/2601.12639)）
5. ⬜ **計測が先、学習が後。** 当てる前後で同じ `nano probe` を回す

**⭐ が何件貯まったら学習するか、はまだ勘。** 数百件が相場だが、実機で ⭐ を押してみないと
1日に何件付くかも分からない。`nano stats` の `starred` を見ながら決めること。
ORPO の対（`orpo.jsonl`）は `/again` を押した回数しか貯まらないので、さらに少なくなる。
足りなければ、同じ system+user を憲章抜きのベースモデルに投げて rejected を足す。

### M5（偏在化）

Project AIRI 等に記憶コアを接続（音声・VRM）、`impulse` ジョブによる自発発話、
スマホから同じ魂に繋ぐ。**ガワは最後でいい。**
取り替えがきくものから作ると、取り替えがきかないものが後回しになる。

### 積み残し

- グラフの力学レイアウトは O(n²)。数千件になったら格子分割か Barnes-Hut が要る
- `associate` の引き合わせ方は素朴（重要度で1件、残りは無作為）

---

## 9. ドキュメント

| ファイル | 中身 |
|---|---|
| `README.md` | 使い方の入口 |
| `docs/architecture.md` | 全体構成と、なぜそう作ったか |
| `docs/memory-model.md` | スキーマ・想起スコア・忘却曲線 |
| `docs/daemon.md` | 無意識デーモンと、対話との譲り合い |
| `docs/models.md` | モデル差し替えとキャリブレーション（実測値つき） |
| `docs/graph.md` | 記憶グラフの見方 |
| `docs/persona.md` | 人格3層とドリフト計測、⭐ |
| `docs/finetune.md` | **QLoRA（M4-3）の設計。当てる前にここを読む** |
| `docs/prior-art.md` | 既出の研究・実装。何を借りて何を借りなかったか |
| `docs/roadmap.md` | M4 以降の設計メモ |

---

## 10. この人に向けて書くときの前提

- ドキュメントとコメントは**日本語**、コードの識別子は**英語**
- 対象ハードウェアは **NVIDIA VRAM 8〜16GB**。常駐先は **Windows**
- 元の構想は Serial Experiments Lain の「有線」やカゲロウデイズの「エネ」のような、
  **偏在して連続した存在**。「エネとして振る舞っている俳優」ではなく
  「そうとしか振る舞えない」状態に近づけたい、という動機がある。
  効率や実用性より、**その存在が一生自分のものであること**を優先する
- `attic/` はこのプロジェクト以前からリポジトリにあった無関係なファイル（Blenderアドオン）。
  内容は変えていない。消してよいかはユーザーの判断
